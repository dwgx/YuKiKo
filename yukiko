#!/usr/bin/env bash
set -euo pipefail

ROOT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
export YUKIKO_ROOT="$ROOT_DIR"
export YUKIKO_SERVICE_NAME="${YUKIKO_SERVICE_NAME:-yukiko}"
exec /usr/bin/env bash "$ROOT_DIR/scripts/yukiko_manager.sh" "$@"

# YuKiKo 当前接手状态（2026-08-08）

> 唯一当前 handoff。旧接手页 `TAKEOVER-2026-08-07.md` 已归档（5 个失败测试已修复、全量恢复绿）。重构蓝图在 `docs/zh-CN/RECONSTRUCTION-BLUEPRINT.md`（v0.2，含实施进度）。
>
> 本轮是**代码落地**，不是设计讨论。以下"验证"均来自本地全量测试，未做真机群聊验证。

## 1. 当前状态

| 项目 | 结论 | 证据 |
|---|---|---|
| 分支 / HEAD | `refactor/prompt-driven-intent`（重构全部完成，语音 base64 直发） | 工作树干净（config.yml / .env 是 gitignored 运行时配置，已 push 远程可拉取） |
| 全量测试 | **2291 passed / 15 skipped / 0 failed** | `.venv/bin/python -m pytest -q` |
| takeover 自检 | status=PASS | `.venv/bin/python scripts/project_takeover_selfcheck.py` |
| 8081 / NapCat | **平台主路径**：`platform.onebot11.primary=true`，NapCat 连 OneBot11Adapter WS，WebUI 同端口。语音 base64 直发已验证（retcode 0） | **API**：config.yml `api` 段 base_url=`https://k1ro.skiapi.dev`（skiapi）、model=`deepseek-v4-flash`、fallback=`[deepseek-v4-flash, deepseek-v4-pro]`，`.env` SKIAPI_KEY=k1ro key。启动前必须 `export SKIAPI_KEY="$(grep '^SKIAPI_KEY=' .env | cut -d= -f2-)"`（否则继承 shell 旧 key 401）。**NapCat**：QQ 带 `--no-sandbox` 启动，注入点 `loadNapCat.js` + package.json main 指向它（还原电脑后需重新注入，见 §11） |
| 蓝图 | `docs/zh-CN/RECONSTRUCTION-BLUEPRINT.md` v0.2 | 含 §4.1-4.6 规格 + §9 OneBot/NapCat 协议 |

## 2. 已落地的重构（本轮全部带回归测试）

| Phase | 落地内容 | 模块/文件 |
|---|---|---|
| 0.5a | 记忆晋升门（rank/consolidate/validate/apply） | `core/memory_promotion.py` |
| 0.5b | 记忆注入 token 预算 + episodic 范例存储 | `core/memory.py`（budget_text_parts / episodic_examples 表） |
| 1a | Hermes 错误回喂自纠（markdown JSON 兜底 + retry_instruction） | `core/agent.py` |
| 1b | GOAP 决策前推理 + prompt 原则（三源同步） | `config/prompts.yml` / `master.template.yml` / `config_templates.py` |
| 1c | loop 实时检测 + post-compaction guard | `core/loop_guard.py` + `core/agent.py` 集成 |
| 3a | SKILL.md 技能加载 + 渐进式披露 + read_skill 工具 | `core/skill_loader.py` + `core/engine.py`/`core/agent.py` 接入 |
| 3b | validate-then-repair 工具调用修复 | `core/tool_call_repair.py` + `core/agent.py` 接入 |
| 3c | MCP 连接器（信任门 + stdio JSON-RPC） | `core/mcp_client.py` |
| 4a | 上下文插件槽（ContextEngine Protocol） | `core/context.py` |
| 4b | 检索双车道（回顾意图 → 强化检索） | `core/recall_intent.py` + `core/engine.py` |
| 5a | PreToolUse 审批钩子（Claude Code hooks 风格） | `core/agent.py` |
| 5b | 轻量 checkpoint（每步工具调用 JSONL） | `core/agent_checkpoint.py` + `core/agent.py` |
| 2 | 平台层骨架（Platform 抽象 + MessageChain + OneBot11 adapter + Manager） | `core/platform/` |

新增回归测试：`tests/test_memory_promotion_phase0_regression.py`、`test_memory_provenance_phase0_regression.py`、`test_memory_injection_budget_phase0_regression.py`、`test_agent_error_recovery_phase1_regression.py`、`test_goap_prompt_principle_phase1_regression.py`、`test_loop_guard.py`、`test_skill_loader.py`、`test_skill_loader_integration_phase3_regression.py`、`test_tool_call_repair_phase3_regression.py`、`test_mcp_client_phase3_regression.py`、`test_context_engine_phase4_regression.py`、`test_recall_intent_phase4_regression.py`、`test_pre_tool_hook_phase5_regression.py`、`test_agent_checkpoint_phase5_regression.py`、`test_platform_phase2_regression.py`。

## 3. 代码事实（读当前工作树）

- `core/agent.py` 工具执行循环现含：`repair_tool_call`（通用修复）→ `_normalize_tool_args`（猜参兜底）→ `loop_guard.veto_if_looping`（critical 阻断）→ `pre_tool_hooks`（审批钩子）→ 执行 → `loop_guard.observe` + `step_journal.record`。失败回喂带 `retry_instruction`（Hermes 式）。
- `core/engine.py`：`skill_registry`（skills/ 目录）+ `read_skill` 工具注册；`user_profile_summary` 三段按预算截断；回顾意图时 `search_related` top_k=8。
- `core/memory.py`：embeddings 表新增 `origin_class` 列（迁移 `_migrate_add_origin_class`）；`_update_user_profile` 对 untrusted（群聊未指向）跳过自动学习。
- `core/platform/`：`components.py`（MessageChain 双向转换）、`base.py`（Platform 抽象 + 状态机）、`manager.py`（生命周期）、`onebot11.py`（反连 WS adapter，事件解析/鉴权已实现，API 分派为骨架）。**未替换 app.py**——真机切换待做。
- `skills/example-skill/SKILL.md`：示例技能，验证加载链路。
- 死代码清理：8 个 `agent_tools_*.py` + `scripts/split_agent_tools.py` 的 `looks_like_preferred_name_knowledge` 未用 import 已删；`utils/learning_guard.py` 定义保留（compat shim，测试保护）。

## 4. 剩余（按优先级）

0. **URL 意图已解读 + 已落地**：`opencode.ai/zen/go` → 用户确认"接入 Zen 模型通道"，已接入（config.yml api 段指向 Zen，`deepseek-v4-flash-free` 真实回复验证通过）。蓝图 §10 保留 OpenCode 架构对照。
1. **第 2 轮 QA Medium 已全部修完（3 路 QA agent 独立审查 + 修复）**：
   - `search_zhihu`：mode 加 enum，search/answers 缺参时返回具体错误（missing_query / missing_question_id），null 视为缺参。
   - `get_qq_avatar`：空串 qq 不再被 registry 拒（空串非必填跳过校验），回退当前用户生效。
   - `translate_en2zh`：schema words 改 string（兼容 list），junk 值（"None"/"[]"）视为缺参不再真调 API。
   - `delete_message`：**自助撤回落地**——普通用户可撤回机器人自己发的消息（agent 层门 + registry 权限门 + handler 归属校验三层对齐），他人消息仍拒。
   - `parse_video`：抖音用户主页 `/user/` 提前拒绝（不再烧 9s），`?modal_id=` 合法视频不误拒。
   - 回归测试：`tests/test_qa_medium_round2_regression.py`（26 例）。
2. **memory_promotion 完整晋升后台任务**：`_run_memory_promotion` 已接线（`_after_reply` 节流触发 + `memory.promotion_enable` 默认 true），但**真机运行验证**（每 50 条消息 consolidate）未专门观测——bot 运行中会自然发生。
3. **已提交**：全部工作已提交（里程碑 `3393921` 重构 + `9711922` 平台主路径 + `557f27b` 全工具 QA 修复 + 第 2/3 轮 QA 修复 + 文档 commits）。工作树干净。

**QA 修复累计**：
- 第 1 轮 9 个（commit `557f27b`）：music 歌名/歌手拆分、memory_add/update 撞唯一索引崩溃、recall_about_user 跨用户泄漏、set_group_ban 目标解析过严、smart_download 上传绕权、QQ 5 位号、get_user_portrait 关键词、music_search limit、search_media 失败 display（`tests/test_qa_fixes_regression.py` 7 例）。
- 第 2 轮 5 个 + 3 路 QA agent 追加：search_zhihu 条件必填、get_qq_avatar 空串回退、translate_en2zh string 化、delete_message 自助撤回、parse_video 抖音主页 + agent 层门 + `?modal_id=` 不误拒（`tests/test_qa_medium_round2_regression.py` 26 例）。
- 第 3 轮 4 路 QA agent 独立审查 social/memory/knowledge/utility/admin/napcat 组，修 8 类：
  - **全局**：`_coerce_basic_type` string 参数 None→空串（根治 null→"None" 空参陷阱）、数组→逗号串（根治 "['a','b']" 脏 repr 标签）。
  - **安全**：`nc_get_rkey` 凭证外泄脱敏（只报存在性）、add_request approve 默认拒绝 + 必填、preferred_name 不再绕过 safety_review、QZone 5 工具挂 super_admin、set_qq_profile 移入 super_admin。
  - **数据**：qzone like_count 读真实点赞 likenum + 新增 forward_count 字段（不再把转发数当点赞数）。
  - 回归测试：`tests/test_qa_round3_regression.py`（8 例）+ 更新 `test_napcat_credential_redaction_regression.py`。

**已知限制（第 3 轮评估后暂不改）**：
- 表情库工具 learn_sticker/correct_sticker/scan_stickers 无权限门——普通用户贡献/修正表情是设计功能，改动会破坏交互。
- 表情发送直连 call_napcat_api 绕过 app.py 发送保护（限流/熔断/暂停）——改发送路径风险高，需真机验证，标记为后续。

## 5. 点歌语音无法播放（已修，需真机验证）

**根因**：QQ 语音消息必须是 **silk 编码**且单条限 ~60s。app.py 原来把 mp3 直接作为 record 发送（点歌配置 `voice_send_music_force_full=true` + `voice_send_music_disable_split=true`，整段发完整歌曲 mp3），NapCat 不保证自动转码，导致语音点开无法播放。

**修复**（commits `42c8980` + `f092467`）：app.py 新增 `_silk_encode_for_record_sync`（ffmpeg 解 PCM + pilk/pysilk 编 silk，截断到 `voice_send_max_seconds`），在**完整直发 / 分段 / 兜底**三个发送点发送前统一转 silk。silk 时长探测用 `pilk.get_duration`（ffprobe 读不出 silk），合规缓存 silk 直接复用。回归测试 `tests/test_voice_silk_regression.py`（5 例）。

**分段配置**（commit `5156e6b`，三源同步：模板 / `config_templates.py` built-in / `config.yml`）：
- `voice_send_try_full_first: false`（长音频跳过整段直发）
- `voice_send_split_enable: true`（允许分段）
- `voice_send_split_max_segments: 8`（最多 8 段）
- `voice_send_music_force_full: false` + `voice_send_music_disable_split: false`（点歌走分段）

点歌长歌曲默认分段发（每段 60s silk，QQ 原生可播）；短音频仍整段直发。

**真机验证（2026-08-08 晚，已通）**：语音已能发出——**base64 直发方案**（见 §10）。

## 6.5 语音发送最终根因：QQ 沙盒 + NapCat ffmpeg 不可用（已修，base64 直发）

**根因链**（macOS QQ 是沙盒应用 `com.apple.security.app-sandbox`）：
1. NapCat 的 ffmpeg native addon（`ffmpegAddon.darwin.arm64.node`）**未签名**被 macOS 拒载（Team ID 不匹配；ad-hoc 签名无效）
2. **沙盒禁止 `execFile` 外部二进制** → 命令行 ffmpeg 检测失败（NapCat 日志 `[FFmpeg] 未找到可用的 FFmpeg`）
3. **沙盒读不到 `file://` 项目路径** → NapCat `isSilk` 读文件失败 → 走 `convertToNTSilkTct` 转换 → ffmpeg 不可用 → **"语音转换失败"（retcode=200/1200）**

**修复**（commit `36ec439`）：NapCat 对 **base64** 解码到**容器内临时文件** → `isSilk` 识别为标准 silk → **直接上传（免 ffmpeg）**。`response_delivery._send_voice` 语音（完整直发+分段）改为 `_send_record_base64` 直接 base64 发送。**真机验证：retcode=0，语音可播放。**

**多连接修复**（commit `ad4f047`）：第一个连接固定为 NapCat（`_napcat_ws`），额外调试连接独立接受（可发 API 不替换 NapCat），`_handle_api` 响应回源——修复调试连接导致 `_send_api` 自环。

**验证通道**：`websockets` 直连 bot ws（`Authorization: Bearer <token>` + `X-Self-ID`），发 `send_group_msg` record `base64://<silk>`。曾用此通道直发阴三儿《马力瓦纳》5 段 + 《上帝为何要这样》5 段（B 站 BV1N4411z7ai 提取）全部 retcode=0。

## 6. 平台主路径 ws 全拒 403（已修，bot 已上线）

**根因**：FastAPI 0.141 的 `@app.websocket` 在真实 uvicorn 0.50 下对 ws 升级**提前 close(403)**——NapCat 永远连不上 8081（`connection rejected (403)` 来自 websockets 库；TestClient 测不出，只影响真实连接）。之前"真机 smoke 15/15"用的是旧 FastAPI/uvicorn 组合，本次重启暴露。

**修复**（commit `a17f817`）：`core/platform/run_primary.py` 改用 **Starlette `WebSocketRoute`** 挂 `/onebot/v11/ws`（`app.router.routes.append(...)`），HTTP 仍走 FastAPI（WebUI 不受影响）。

**真机验证（2026-08-08）**：
- 带 token ws 连接 OK，`get_version` 返回 `{"status":"ok","retcode":0}`。
- **NapCat 已连上**：`QQ Helper 28662 → 127.0.0.1:8081 ESTABLISHED`。
- bot 当前运行：PID 55971（平台主路径）。

**点歌语音**：修复后 NapCat 连通，点歌应能发送；结合 §5 的分段配置，长歌分段下发。待群聊实测。

## 5. 下一步最小命令

```bash
cd /Users/dwgx/Documents/Project/YuKiKo
git status --short
.venv/bin/python -m pytest -q
.venv/bin/python scripts/project_takeover_selfcheck.py
```

真机 smoke 需先：修 NapCat（`--no-sandbox`）→ 机器人监听 8081 → owner 授权群号 → 才可跑 `tool_smoke_live.py --real-group`。

## 7. 架构收敛（4 路评审 + 2 批并发，已提交）

**评审结论（4 路 agent）最垃圾的架构地方**：
1. 配置=四处真相（模板/built-in/config.yml/消费端 get 默认 682 处），`deep_merge_dict` 只回填不校验，漂移静默错
2. 三套发送实现（app.py 全量 / run_primary 简化 / bridge._reply_to_session 无限流熔断）互相漂移
3. God function（engine.handle_message 1354 行、agent.run 1487 行、app.register_handlers 2222 行、navigator 2329 行纯数据）
4. 工具双轨穿墙（agent 工具调 router 私有 `_method_*`，权限只有一边有）

**已收敛（5 批提交 `8132af4`→`00a04fb`，全量 1994 passed）**：
- **A 配置单一真相**：`core/config_schema.py` 启动校验 + **strict 模式**（`YUKIKO_CONFIG_STRICT=1`/`validation.strict` 抛 ConfigValidationError）；修复 private_chat_mode 类型矛盾、routing 阈值、61 键回填 built-in；schema 与模板全量一一对应（354 键）；navigator 2329 行数据移 `core/prompt_navigator_data.yml`（prompt_navigator 2821→564 行）
- **B 单一发送核心**：`core/response_delivery.py`（语义拆分+限流/熔断/暂停+媒体+语音分段），平台路径 + app.py 发送限流统一改用；删约 253 行手写重复
- **C 工具单轨**：拆 9 个 call-through 包装直接调底层公开函数；7 个能力描述双源统一为常量
- **D 大文件瘦身**：纯函数搬到 `core/media_utils.py` + `core/text_utils.py`；`handle_message`（1354→1334）、`agent.run`（1487→1467）、`app.register_handlers`（2222→2146）保守垂直切片
- **平台入站统一**：sender_role（群主/管理员权限可判）、reply/at 上下文（get_msg 解析回复媒体、回复 bot mentioned）、dedup/媒体缓存确认在 engine 层生效

**体量变化**：agent.py 6596→~6000、engine.py 7634→~7100、app.py 3162→~3000、prompt_navigator 2821→564

## 8. 下一阶段重构（蓝图差距补齐，4 批已提交）

**第一批 `38de84c`**：E1 handle_message TurnContext + 14 意图谓词搬 `intent_predicates.py`；E2 agent 提取 `_record_guard_block`/`_resolve_final_answer_media`/`_check_permission_gate`（run 1467→1385）；E4 点歌语音 4 特性迁 `response_delivery._send_voice`（app.py 语音段 220→55 行）+ wiring 抽共享 `build_message_handler`；E5 conftest `make_engine` 工厂 + handle_message 全流程 + loop_guard 接线 + 熔断分支测试
**第二批 `7be09aa`**：E3 热重载接线（reload_config 重建 search/affinity/skill_registry/sticker，不重置 _async_init_done）；E6c WebUI `/memory/summary`（分层统计）、`/memory/records?origin_class`、`/skills` 端点
**第三批 `b9cbd14`**：E6b 蓝图 §4.5 deny 即移除工具（会话级 deny 集，连续失败≥3 或 loop critical 时从模型可见 schema 剔除，30 分钟惰性恢复）；E3b 热重载回归测试
**第四批 `f1dd260`**：E6a checkpoint 自动恢复（agent.run 支持 resume_checkpoint 续跑 + queue 超时 resume_token + 修 checkpoint 落盘时机缺陷）；C1c 剩余 5 个 call-through 清零；P9 蓝图 §9（image URL 2h 过期检测、get_group_file_url group→group_id、mface 入站跳过）

**全量 2156 passed / 0 failed**。

**第五批 `69d9474`（剩余项补齐）**：
- E6a-end：`EngineMessage.resume_checkpoint_id` 消息级传递 + `_try_agent_path` 传 AgentLoop + 兜底 resume token 会话级存储（meta 带出），端到端续跑链路打通（自动重试留真机）
- B5：`main.py` 入口统一 `_select_entry_path`（primary true 走 run_primary，缺失回退 legacy 并注明翻默认风险）
- Z1（§10）：`services/model_quality.py` 模型质量跟踪打分（成功率 0.6 + 延迟 0.3 + 错误惩罚 0.1），`rank_failover` 默认关闭只记录
- M1（§4.6）：记忆三段注入护栏补齐（memory_context/related_memories 字符预算，`budget_text_lines` 新增）

**剩余（仅剩真机项）**：
- checkpoint 端到端自动重试（queue 联动，需真机）
- 平台双轨最终收敛（翻默认需模板回填 platform 段 + 真机验证）
- 记忆晋升后台任务真机观测

## 9. Hermes/OpenClaw/Reflection_King 对比强化（3 批已提交）

**研究结论**（4 路 agent）：Hermes 记忆（写入安全/反思环/FTS5/自主整理）、OpenClaw 工具扩展（运行时加载/Skill Workshop）、Reflection_King 抓取（候选评分/多解析器/SSRF）是主要差距。

**爬虫/抓取**（`84368e1` + `b876aca`）：
- SSRF：IPv4-mapped/6to4/NAT64/Teredo 折叠回 v4 规则（防 ::ffff:169.254.169.254 绕过）
- 正文净化：stdlib HTMLParser 去噪/主块密度选块/移动端无 p 提取/site_name/2000 字
- **小红书解析器**（core/xhs.py）：图文多图 + 视频直链 + og 兜底 + 登录墙识别 + cookie 注入
- **候选评分**（core/candidate_scoring.py）：kind 基础分/分辨率/码率/广告扣分/evidence 加分
- **多解析器聚合**：yt-dlp 失败后 you-get/streamlink --json 探测，URL 去重选优

**记忆（对比 Hermes）**（`b876aca` + `a769746`）：
- **写入安全**（core/memory_guard.py）：注入 warning/密钥泄露 critical 拒写/隐形字符剥离
- **回合级反思环**：每 15 条/10 分钟便宜模型提炼 JSON 事实→provenance=agent.reflection，幂等
- **FTS5 会话全文检索**：embeddings_fts 镜像 + 触发器同步 + 向量无结果兜底
- **自主整理**：memory_list 超 200 条提示删除/compact

**工具扩展（对比 OpenClaw）**（`a769746`）：
- **运行时动态加载**：AgentToolRegistry.unregister + reload_config 重跑 MCP sync + WebUI 端点
- **Skill Workshop**：create_skill/remove_skill/list_skills（super_admin，SKILL.md 模板 + reload 立即可见）

**剩余（可选/边际）**：
- OpenClaw P1 manifest/分发（最小 manifest + git 源安装）、P2 optional allowlist、P2 MCP streamableHttp
- Hermes P2 agent 人格记忆（agent 级 MEMORY 注入）
- 快手解析依赖 yt-dlp 第三方插件（官方不支持，解析不稳）
- prompt_navigator 把 create_skill 排除在分区工具外（O2 建议接线）

**注意**：2026-08-08 bot 曾因**系统内存不足（8GB 用 97%）导致 QQ 崩溃**，且 QQ 崩溃后 **NapCat 注入被还原**（package.json main 变回默认），需 `sudo cp loadNapCat.js → /Applications/QQ.app/Contents/Resources/app/` 重新注入 + 重启 QQ（`bash scripts/napcat_macos.sh`）。

## 11. 还原电脑后恢复清单（2026-08-09）

**代码（git 远程可拉）**：分支 `refactor/prompt-driven-intent` 已 push 到 `origin`。还原后：
```bash
git clone <remote-url> YuKiKo
cd YuKiKo && git checkout refactor/prompt-driven-intent
python scripts/deploy.py          # 建 .venv + 装依赖
```

**必须重新配置的（gitignored，远程没有）**：
1. **`.env`**（`cp .env.example .env` 后填）：
   - `SKIAPI_KEY=`（k1ro: `sk-kiro-...`）
   - `ONEBOT_ACCESS_TOKEN=`（与 config.yml `platform.onebot11.access_token` 一致）
   - `WEBUI_TOKEN=`（WebUI 登录）
2. **`config/config.yml`**：无此文件时 `python main.py --setup` 走向导；关键段：`bot.nicknames=[雪,yuki,yukiko,雪酱]`、`api.base_url=https://k1ro.skiapi.dev`、`api.model=deepseek-v4-flash`、`control.memory_recall_level=medium`、`platform.onebot11`（primary/host/port/access_token/bot_id=`2488687937`）、语音分段配置（`voice_send_try_full_first=false` 等）
3. **`storage/`**（记忆/知识库 SQLite，gitignored）：还原前先备份，否则记忆/知识丢失
4. **NapCat 注入**（QQ 还原后 package.json 被还原）：
   ```bash
   sudo cp ~/Library/Containers/com.tencent.qq/Data/Documents/loadNapCat.js \
       /Applications/QQ.app/Contents/Resources/app/loadNapCat.js
   # 改 package.json main = "./loadNapCat.js"（sudo）
   bash scripts/napcat_macos.sh    # 写配置 + --no-sandbox 重启 QQ
   ```

**启动 bot**：
```bash
export SKIAPI_KEY="$(grep '^SKIAPI_KEY=' .env | cut -d= -f2-)"
nohup .venv/bin/python main.py > /tmp/yukiko_bot.log 2>&1 &
```

**语音 base64 直发**（代码内置，无需配置）：NapCat ffmpeg 不可用（QQ 沙盒），语音走 base64 直发（§6.5）。还原后无需额外处理。
